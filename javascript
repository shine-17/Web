# 스코프(Scope)
Scope를 우리말로 번역하면 ‘범위’라는 뜻을 가지고 있다.
즉, 스코프(Scope)란 ‘변수에 접근할 수 있는 범위’
자바스크립트에선 스코프는 2가지 타입이 있다. 바로 global(전역)과 local(지역)

# Scope - var, let, const 차이
var
- 함수 레벨 스코프이며 변수 재할당, 재선언이 가능
- 버그 발생과 메모리 누수의 위험
let
- 변수 재할당 가능, 변수 재선언 불가능
const
- 변수 재할당 불가능, 변수 재선언 불가능
